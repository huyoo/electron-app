{
  "name": "electron-app",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager ~/electron-app/build electron-app --all --out ~/ --electron-version 6.0.9",
    "pack": "electron-packager . electron-app --win --out ../electron-app --arch=x64 --app-version=1.0.0 --electron-version=6.0.9 --download.mirror=https://npm.taobao.org/mirrors/electron/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "huyoo",
  "license": "ISC",
  "devDependencies": {
    "electron": "^7.2.4",
    "electron-packager": "^14.0.6"
  },
  "homepage":"."
}
